overwrite: true
schema: 'http://localhost:5000/graphql'
config:
  avoidOptionals:
    field: true
    object: true
    defaultValue: true
  maybeValue: T | null
generates:
  src/generated/fragments.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
